{
  "name": "openbims-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Generated by /openbims_video. Per-module video assets, Playwright recipes, HyperFrames compositions.",
  "scripts": {
    "record": "playwright test",
    "record:headed": "playwright test --headed",
    "preview": "npx hyperframes preview",
    "render": "npx hyperframes render",
    "lint": "npx hyperframes lint"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
