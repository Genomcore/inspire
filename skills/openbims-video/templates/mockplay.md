# {{SUBMODULE}} — Playwright recipe

> Generated by `/openbims_video mockplay {{MODULE}}`. Maps the script's beats
> for this submodule to concrete Playwright actions on the running console.

## Pre-conditions

- Console dev server reachable at `${CONSOLE_URL:-http://localhost:5173}`
- Mock data tables loaded:
  - `mock-data/tables/{{TABLE_1}}.jsonl`
  - `mock-data/tables/{{TABLE_2}}.jsonl`

## Hero entities

<!-- The specific rows the recording follows. Pick rows with the most
     populated fields and the most realistic IDs/names — these end up
     on screen and define the viewer's first impression. -->

| Field           | Value                            |
| --------------- | -------------------------------- |
| `{{TABLE}}.id`  | `{{HERO_ID}}` ({{HERO_NAME}})    |
| `{{TABLE}}.id`  | `{{HERO_ID}}` ({{HERO_NAME}})    |

## Beats

<!-- Each beat is one focused interaction. Aim for 5–10s per screen.
     Use the `caption_id` to bind to the matching caption in script.md. -->

```yaml
beats:
  - id: land
    submodule: {{SUBMODULE}}
    route: /{{module}}/{{path}}
    action: wait-for-table
    selector: 'table tbody tr'
    duration_ms: 1500
    caption_id: t=0

  - id: focus-row
    action: focus
    selector: 'table tbody tr:has-text("{{HERO_NAME}}")'
    duration_ms: 1200
    caption_id: t=2

  - id: open-detail
    action: click
    selector: 'table tbody tr:has-text("{{HERO_NAME}}")'
    wait_for: networkidle
    duration_ms: 1500
    caption_id: t=4

  - id: scroll-detail
    action: scroll
    delta_y: 280
    duration_ms: 900
    caption_id: t=6

  - id: tab-skills
    action: click-tab
    name: Skills
    duration_ms: 1400
    caption_id: t=8
```

## Expected duration

`{{N}}s` (= sum of `duration_ms`)

## Selectors notes

- Prefer `getByRole` + accessible name where the element exposes a role.
- For data rows, use `:has-text("{{stable-string}}")` against the hero entity.
- If the page lacks robust selectors, file a follow-up to add `data-testid`
  rather than building fragile XPath here.
