---
id: auth::user::create
lifecycle: accepted
---

# auth::user::create

## Purpose

The action simply hashes the password before the write.

Of course the retry is seamless, and the caller sees one call.

The `simply` column names the caller, and `merely` is a quoted token.

The queue drains into the pipeline that the tenant owns.
