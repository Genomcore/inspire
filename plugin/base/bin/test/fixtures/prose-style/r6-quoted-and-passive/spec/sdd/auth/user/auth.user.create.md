---
id: auth::user::create
lifecycle: accepted
---

# auth::user::create

## Purpose

The salt is used to derive the key, and the tenant column was used to hold it.

The `previously` column names the prior owner, and `used to` is a quoted token.

The caller used to send a cookie.
