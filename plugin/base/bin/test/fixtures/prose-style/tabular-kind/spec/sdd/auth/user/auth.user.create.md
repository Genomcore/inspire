---
id: auth::user::create
lifecycle: accepted
---

# auth::user::create

## Inputs

The invoice payment capture retry policy configuration decides which parameter of the two below the caller has to send along with the request, and the table names both of them.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `email` | email | yes | The address that was previously stored. |
