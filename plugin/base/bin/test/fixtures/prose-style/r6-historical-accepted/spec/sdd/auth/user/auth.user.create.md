---
id: auth::user::create
lifecycle: accepted
---

# auth::user::create

## Purpose

The total was previously computed on the client.

The tenant used to own the setting.

The field migrated from the old table.

The ~~legacy header~~ has no reader.
