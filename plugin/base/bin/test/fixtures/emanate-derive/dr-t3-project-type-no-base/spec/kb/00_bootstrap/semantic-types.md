# Semantic types — the project's own

| Type | Base type | Means / predicate | Rendering |
|------|-----------|-------------------|-----------|
| `status` | `moolah` | One of the account states. | — |
