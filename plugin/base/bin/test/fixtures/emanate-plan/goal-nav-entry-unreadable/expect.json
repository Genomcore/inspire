{
  "exit": 0,
  "args": [
    "--profiles-root",
    "spec/profiles",
    "--agents-root",
    "spec/agents",
    "--goal",
    "users.detail"
  ],
  "stdout_jq": [
    { "expr": ".ready", "equals": "true" },
    { "expr": ".goal.units | join(\",\")", "equals": "users.detail,users.list" },
    { "expr": ".goal.floor", "equals": "1" },
    { "expr": ".floor", "equals": "1" },
    { "expr": ".waves | length", "equals": "1" },
    { "expr": "[.findings[].code] | join(\",\")", "equals": "PR-24" },
    { "expr": "[.findings[].severity] | join(\",\")", "equals": "info" },
    { "expr": ".findings[0].owner", "equals": "inspire-screens" },
    { "expr": ".findings[0].target", "equals": "spec/kb/05_screens/users/home.md" },
    { "expr": ".findings[0].message | test(\"is unknown\")", "equals": "true" },
    { "expr": ".findings[0].message | test(\"withheld rather than guessed at\")", "equals": "true" }
  ],
  "forbidden": ["PR-23", "PR-11", "NOT READY"]
}
