{
  "exit": 0,
  "args": [
    "--profiles-root",
    "spec/profiles",
    "--agents-root",
    "spec/agents"
  ],
  "stdout_jq": [
    { "expr": "[.findings[].code] | unique | join(\",\")", "equals": "PR-24" },
    { "expr": ".findings[0].severity", "equals": "warning" },
    { "expr": ".findings[0].owner", "equals": "inspire-bootstrap" },
    { "expr": ".findings[0].target", "equals": "spec/kb/00_bootstrap/stack.md" },
    { "expr": ".findings[0].unit", "equals": "null" },
    { "expr": ".ready", "equals": "true" },
    { "expr": "[.preflight.components[].name] | join(\",\")", "equals": "postgres" },
    { "expr": ".preflight.probe_profiles | join(\",\")", "equals": "nestjs" },
    { "expr": ".preflight.worktree_recipe | length", "equals": "0" }
  ],
  "forbidden": ["PR-22", "NOT READY"]
}
