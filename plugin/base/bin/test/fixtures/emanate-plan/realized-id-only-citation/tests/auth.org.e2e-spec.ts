// Every claim cited, none with a fingerprint. Valid for the gate's coverage
// classes and worth nothing to realization: an id-only token says someone tested
// the claim, never which version of it.
describe('auth.org', () => {
  // @claim auth.org/field/id/nonnull
  // @claim auth.org/field/id/unique
  // @claim auth.org/field/id/immutable
  // @claim auth.org/field/slug/nonnull
  // @claim auth.org/field/slug/len
  it('is delivered', () => {})
})
