---
kind: bootstrap-stack
status: active
profiles: [ios, typescript]
---

# Tech stack

The language profile IS declared here, and `PR-06` still fires: the check is per
framework, and `ios.md` names no `language:` of its own. Which is why the remedy
offers only per-framework repairs — an operator who declared a language profile
in this file has already done the thing the older wording suggested, and nothing
changed.

## Language

- **Swift**, and no `swift` language profile ships.
