---
kind: inspire-code-profile
id: ios
layer: frontend
---

The framework profile for a native iOS surface. It declares NO `language:` at
all — no `swift` language profile ships — so nothing states how a semantic type
renders and every unit resolving it refuses at t=0.

## Layering

Not yet formulated for this stack.
