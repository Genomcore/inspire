---
kind: bootstrap-stack
status: active
profiles: [typescript]
---

# Tech stack

A language profile and nothing else. R8' dropped the count-based refusal, but an
empty framework set is an absence rather than an ambiguity: nothing states an
architecture to emanate this entity under, so the arm that refuses it stays.

## Language

- **TypeScript**, end to end.
