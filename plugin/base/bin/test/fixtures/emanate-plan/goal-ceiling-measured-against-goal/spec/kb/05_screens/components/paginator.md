# Component: paginator

**Purpose:** move through a collection one page at a time.
**State:** to-extract

The pager the roster layout injects into its status region.

## API / Slots

| Prop / slot | What it carries |
|------|--------------------------|
| `total` | how many records there are in all |
| `on-page` | the handler a page change calls |
