# Component: list

**Purpose:** render many records of one kind as rows.
**State:** to-extract

The row list the roster layout injects into its body.

## API / Slots

| Prop / slot | What it carries |
|------|--------------------------|
| `rows` | the records to render |
| `on-row-click` | the handler a row activation calls |
