# Component: filter

**Purpose:** narrow a collection by one or more facets.
**State:** to-extract

The filter bar the roster layout injects into its toolbar.

## API / Slots

| Prop / slot | What it carries |
|------|--------------------------|
| `facets` | the facets the caller offers |
| `on-change` | the handler a facet change calls |
