---
kind: bootstrap-stack
status: active
profiles: [nestjs]
---

# Tech stack

The stack this fixture's units are emanated under. `nestjs.md` names
`language: react`, and `react.md` is a framework profile of its own — the
pulled-in file exists, and there is still no `layer: language` anywhere in the
resolved set, so nothing states how a semantic type renders.

## Language

- **TypeScript**, end to end.
