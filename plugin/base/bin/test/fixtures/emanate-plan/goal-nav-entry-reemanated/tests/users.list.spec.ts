// Every claim of users.list's current contract, cited with a matching
// fingerprint: the roster screen is delivered, and it is the only entry into
// the three-screen navigation cycle the goal sits in — nothing outside the
// cycle links in, so there is no nav root.
//
// `--reemanate users.list` then puts it back in the frontier for this run.
// That is "rebuild it", not "it was never built", so the reachability class
// reads delivery on disk and must stay silent.
describe('users.list', () => {
  // @claim users.list/data/main sha256:c7d2d4020ca5430dc43feda44ced89790b73ca87876755da571496ba0c625706
  // @claim users.list/nav/row sha256:8fd9be6d595011fe2fe4933972431002d2bfe24078a1ef5c175a7dad02954d61
  // @claim users.list/state/empty sha256:653f0a759630ea1b0c360ffccf7e47cd8538a93b5d9cd0874aeb510c27461b3c
  it('is delivered', () => {})
})
