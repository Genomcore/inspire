---
kind: bootstrap-stack
status: active
profiles: [ios]
---

# Tech stack

A native iOS suite of one. `ios.md` declares no `language:` at all, so the
screen's framework set is a singleton and still reaches no rendering table.

## Language

- **Swift**, and no `swift` language profile ships.
