---
kind: bootstrap-stack
status: active
profiles: [nestjs]
---

# Tech stack

The stack this fixture's units are emanated under. Backend-only: the frontier
here is domain units, whose owning framework no `layer:` states, so a second
framework profile would make every one of them `PR-07`.

## Language

- **TypeScript**, end to end.
