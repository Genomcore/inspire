---
kind: bootstrap-stack
status: active
profiles: [angular]
---

# Tech stack

An Angular suite of one. `angular.md` names `language: typescript` and that file
is a `layer: language` profile, so the screen resolves a rendering table and
nothing refuses.

## Language

- **TypeScript**, end to end.
