---
kind: bootstrap-stack
status: active
profiles: [react]
---

# Tech stack

A React suite of one, so every kind resolves a singleton framework set and the
plan this fixture pins is about the goal's closure rather than about profiles.

## Language

- **TypeScript**, end to end.
