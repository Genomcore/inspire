{
  "exit": 0,
  "args": [
    "--profiles-root",
    "spec/profiles",
    "--agents-root",
    "spec/agents",
    "--tests-root",
    "tests"
  ],
  "stdout_jq": [
    { "expr": ".ready", "equals": "true" },
    { "expr": ".realized | join(\",\")", "equals": "auth.org" },
    { "expr": ".floor", "equals": "1" },
    { "expr": "[.waves[].units[].id] | join(\",\")", "equals": "auth.user" },
    { "expr": "[.waves[] | select(any(.units[]; .id == \"auth.user\")) | .wave] | join(\"\")", "equals": "1" },
    { "expr": "[.waves[0].units[0].requires[].id] | join(\",\")", "equals": "auth.org" },
    { "expr": "[.findings[].code] | join(\",\")", "equals": "" }
  ],
  "forbidden": ["PR-03", "PR-", "NOT READY"]
}
