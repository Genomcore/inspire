{
  "exit": 0,
  "args": ["--contract", "contract.json", "--tests-root", "tests", "--results", "results.json"],
  "stdout_jq": [
    { "expr": ".claims[0].status", "equals": "covered" },
    { "expr": ".claims[0].citations | length", "equals": "2" },
    { "expr": "[.claims[0].citations[].file] | sort | join(\",\")", "equals": "tests/a.spec.ts,tests/b.spec.ts" }
  ]
}
