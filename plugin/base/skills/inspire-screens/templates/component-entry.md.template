---
# surfaces: [id-a, id-b]   # blast radius — list of roster ids, or `all`; omit = suite-wide; see `.claude/skills/_references/surface-scope.md`
---

# Component: {component-name}

**Purpose:** {one-line description of what this component is for — first body line, greppable}
**State:** implemented | to-extract

{One-paragraph description of the component.}

## Structure

1. {Structural element 1}
2. {Structural element 2}

Tokens (spacing, type, colors, density) come from the design system
([`../design-system.md`](../design-system.md)) — do not restate them here.

## API / Slots

| Prop / slot | What it carries |
|------|--------------------------|
| `{prop}` | {what it carries} |

## States

<!-- Optional, and keyed the way a screen's `### States` is (Key · When ·
     Presentation). These are the component's OWN renderings — empty, loading,
     error — the ones every screen instantiating it inherits. A state a single
     screen enters belongs to that screen's `### States` instead. Each row is
     one claim the emanation loop covers with a render test. -->

| Key | When | Presentation |
|---|---|---|
| `{key}` | {the condition} | {what the component shows} |

## Variants

- **{Variant name}** — {when to use it}.

## Instances

<!-- While **State** is `to-extract`, list the adopters — the screens/prototype
     routes still holding their own copy — this entry will absorb. -->
- {screen or prototype route using this component}
