---
id: {module}.{screen}
module: {module}
screen: {screen}
lifecycle: draft
---

<!-- Required: the frontmatter identity block (id · module · screen · lifecycle),
     the H1 title, the Features line, and ## Bindings with at least one of its
     four subsections (### Data · ### Dispatches · ### Navigation · ### States).
     Optional / presence-free: the Pattern and Components lines,
     ## Module-specific deviations, ## Current prototype, ## Notes.
     This split is the single source of the two lists the validator encodes.

     `id` is minted once and never re-derived from location: the default mint is
     {module}.{screen}, and a newcomer whose default is already taken mints
     {surface}.{module}.{screen}. Moving the file never touches it. The route is
     NOT authored here — it derives from `module` + `screen`; see
     references/format-screen.md. -->
# {Screen Title}

**Features:** FEAT-01, FEAT-02
**Pattern:** [[{rel-to-05_screens}/patterns/{pattern-name}]]
**Components:** [[{rel-to-05_screens}/components/{component-name}]]

## Bindings

<!-- The screen's own semantics, declared whether or not a Pattern is named.
     Keys are screen-local and unique per subsection; each row generates one
     claim ({id}/data/{key} · {id}/dispatch/{key} · {id}/nav/{key} ·
     {id}/state/{key}). Drop a subsection this screen has nothing to declare in.
     A pipe-syntax wikilink inside a table cell escapes its pipe (`\|`) — an
     unescaped one would end the cell. A bare colon-form link is equally valid. -->

### Data

| Key | Action | Notes |
|---|---|---|
| `main` | [[{module}.{entity}.{action}\|{module}::{entity}::{action}]] | {what this feed is for} |

### Dispatches

| Key | Action | Trigger | On success | On error |
|---|---|---|---|---|
| `create` | [[{module}.{entity}.{action}\|{module}::{entity}::{action}]] | {what the user does} | → [[{target-screen-id}]] | state `{state-key}` |

### Navigation

| Key | Target | Trigger |
|---|---|---|
| `back` | [[{target-screen-id}]] | {what the user does} |

### States

| Key | When | Presentation |
|---|---|---|
| `empty` | `main` returns zero rows | {what the user sees} |

<!-- Optional — only when the screen deviates from the pattern's defaults, or
     when which component fills which region needs saying. -->
## Module-specific deviations

- {describe only what deviates from the pattern defaults}

<!-- Optional — leave empty when aligned; omit the section entirely until a prototype target exists. -->
## Current prototype

- **Target:** {prototype route(s) realizing this screen — the derived route, shell-prefixed in a multi-UI suite — or `none yet`}
- **Drift:** {informational misalignments between the prototype and this spec; leave empty when aligned}
  - {ADR alignment | data wiring | component adoption | gap | cosmetic} — {what differs}

<!-- Optional — presence-free. -->
## Notes

- {domain-specific behavior, edge cases, user feedback that informed the design}
