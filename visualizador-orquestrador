#!/usr/bin/env bash
# Browse an emanation run: `bash visualizador-orquestrador [--session <id>]` from
# anywhere. Read-only over `.inspire/emanate-runs/`; writes nothing.
set -euo pipefail
cd "$(dirname "$0")"
exec python3 visualizer/cli.py "$@"
