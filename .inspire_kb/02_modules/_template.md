---
kind: module-hub
prefix: MYM              # the module's feature / use-case ID prefix
---

# {Module Name}

> The module **hub** — overview, relationships, and links to everything this module
> owns across the layers. Kept in sync with the per-layer subfolders.

## Overview

{What this module is and does, in a few sentences.}

## Relationships

{How it relates to other modules — dependencies, shared entities, upstream/downstream.}

## Use cases

_Index of the features in [`../03_features/{module}/`](../03_features/{module}):_

- [[../03_features/{module}/{use-case}|{USE-CASE-ID}]] — {one line}

## Screens

_[`../05_screens/{module}/`](../05_screens/{module}) — the module's screens; the route
map + coverage table live in that folder's `_index.md`._

## Domain

_[`../04_domain/{module}/`](../04_domain/{module}) — action descriptors + entity
documents for this module._

## Module ADRs

_Decisions scoped to this module (`adr-{prefix}-*` in [`../01_adr/`](../01_adr)):_

- [[../01_adr/adr-{prefix}-{slug}]] — {relation}
