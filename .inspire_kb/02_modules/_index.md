# Modules — registry

_Every module in the product. One hub file per module in this folder; add a row here
when `/inspire_module create` scaffolds a module._

| Module | Prefix | Hub |
|--------|--------|-----|
| _e.g._ Auth | `AUTH` | [[auth]] |
