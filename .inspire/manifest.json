{
  "name": "inspire",
  "version": "0.2.0",
  "released": "2026-07-28",
  "description": "INSPIRE runtime release identity. Bump `version` (semver) + `released` (YYYY-MM-DD) on each release; install.sh freezes these into a fork's root .inspire.lock, and inspire-lesson stamps them onto every lesson in .inspire_kb/98_lessons/."
}
